import 'package:flutter/material.dart';

class TripPlanner extends StatelessWidget {
  const TripPlanner({super.key});
  
  @override
  Widget build(BuildContext context) {
    return AppBar();// FOR DEBUGGING REMOVE LATER
  }
}